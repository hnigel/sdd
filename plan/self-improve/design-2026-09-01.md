# spec-pipeline 自我改進 — 出貨規格（定案批）

- 日期：2026-09-01（v7，拆分後的出貨版）
- 規劃：Fable（owner 指定；規劃者/監督者拆開，owner 授權）。任務：「用自己的 skill 優化自己，依據是實際執行的 log」＋跨 macOS/WSL 硬需求。
- 本檔**只含 run1（R1-R3）與 run2（R1-R3）六輪審查中從未被實質反對的項目**。green 語意重設計（六輪 churn 全集中處）整批拆出 → `design-green-semantics-2026-09-01.md`（不送審、不實作，日後另走流程）。owner 裁示紀錄見 §6。

## 0. 未修事項聲明（先讀這個）

**`green_allowed` 的收口判定仍是自我回報，本規格不修。** `review-state.mjs:154`（`openBlockers = filter(!resolved)`）＋`:329`（`green_allowed: open.length === 0`）：`--resolve` 把最後一輪的 BLOCKER 全部自我宣告完，無需任何複審輪即放行。這是已驗證的真實缺陷（F10 的操作者靠人工否決它才沒出事），但六輪審查證明「怎麼判定 GREEN」這一層尚未收斂——三版機制（revision 綁定、marker、VERDICT）全被打掉，其一還是審查者自提後自行撤回的。強行出貨只會再立一道製造錯覺的閘。**過渡措施只有 C1-6 的一行提示輸出（不是閘門）。** 操作規則：看到 `green_allowed: true` 且收口靠 `--resolve`，一律再送一輪複審——這正是 F10 操作者實際做的事。完整失敗史與候選設計在第二份檔案。

## 1. S0 與事實

S0：`hard`（放行判準腳本的正確性 + 跨平台且 macOS 不可本機實測 + 入口架構取捨）。owner 指定 Fable，一致。以下事實除註明者均為本機第一手（2026-09-01），行號經實地開檔核對：

- **F12 輪數上限可繞過**：`record()` 先 push（`review-state.mjs:230`）、save（`:236`）；CLEAR 分支（`:241-246`）排在 `MAX_ROUNDS` 檢查（`:247-252`）前。R3 exit 20 後再 record 即 R4，零 BLOCKER 走 CLEAR return；round 在任何檢查前已寫檔。
- **F13 `--blockers` 可鑄造放行輪**（實跑）：`--blockers abc` → NaN → `Array.from({length:NaN})`=`[]`（`:206-209`）→ CLEAR、`green_allowed: true`；`--blockers 0` 同理。監督者與規劃者各自重現過。
- **F14① 凍結閘空轉**：`assertS3Clear()` 在「沒有 review-state.json」（`spec-freeze.mjs:89-92`）與「S3 零輪次」（`:94-97`）都印警告後**照常凍結**——完全沒過 S3 的規格可成為「唯一驗收依據」。
- **F16 狀態寫入無鎖無原子**：`review-state.mjs:60-63`、`spec-freeze.mjs:56-59` 裸 `writeFileSync`；兩個並行 record 可各寫 R3，後寫者覆蓋（本 pipeline 常派並行 agent）。
- **F2/F11 守衛缺失實證**：2026-09-01 01:07 UTC，game_01 一個未收口 run 被 `--start` 覆蓋，僅 stderr 一行在守（`:160-165`）；真實 transcript 逐字：「`--blockers <n>`（無條件覆寫，**本次全程都這樣用**）」——逃生口曾是全程預設且不留理由。
- **F5/F6 跨平台缺陷在散文**：兩份 SKILL.md 共 14 行 `$RS`/`$SF` 依賴 bash 分詞（定義：`codex-review/SKILL.md:138`、`spec-pipeline/SKILL.md:102,183`；呼叫點：`codex-review/SKILL.md:140-144`、`spec-pipeline/SKILL.md:103,105-107,185-186`）；zsh（macOS 預設）不分詞 [zsh 未本機實測；macOS 真實 run 筆記記載此坑]。Node 腳本與測試本身可攜（execFileSync/spawnSync 不經 shell）。`validate-plugin.mjs:45-52` 只驗 frontmatter，無人守散文。
- **F3 契約三條機器事實過期**：`codex-review/SKILL.md:8` 稱驗於 v0.150.0-alpha.7（本機 `codex --version` → 0.144.1）；`:179-181` 稱 config 為 gpt-5.6-sol/xhigh（實際 `cat ~/.codex/config.toml` → gpt-5.5/medium）；`:128-130` 引用的 log 案例本機無對應（真實 log `^codex$` 出現 5 次非 1 次）。
- **F15 漂移繼承活證＋banner 在 stderr**（監督者實跑）：smoke test 不帶 `-c` 當場印 `reasoning effort: medium`；stdout 只有回答本文，banner（model/effort 行）在 stderr——`execFileSync` 預設拿不到。重驗：`echo '只回答兩個字：ok' | codex -C "$PWD" -s read-only -a never exec -m gpt-5.6-sol - > /dev/null` 看 stderr。
- **F4 明寫呼叫點救過真實一輪**：歷史 log header `model: gpt-5.6-sol / reasoning effort: xhigh`（config 已漂移仍用對，因 `codex-review/SKILL.md:33` 明寫 `-m`/`-c`）。
- **F7 cache/repo 分離**：實際載入 `~/.claude/plugins/cache/sdd/spec-pipeline/0.1.0/`；`installed_plugins.json` 兩個 scope（project=game_01、user）都釘 `1f60905`，repo HEAD `214e719`。改 repo → push → owner 手動 `/plugin update` 才生效；已載入舊 SKILL 的 session 不會變。[推測] 同版本號是否刷新 cache 未實測 ⇒ 版本 bump 繞開。
- **F8** `README.md:170`、`HANDOFF.md:26` 寫「44 個 case」，實跑 50。**F9** `review-state.mjs:231` 存揮發 mktemp 路徑，實質證據在 `findings_block`（`:232`）。

## 2. 原則（新增兩條）

**④跨平台**：因機器而異的值一律在呼叫點明寫；散文中會被執行的指令須有機械 lint 守已知不可攜模式；帶版本/機器狀態的事實必附 30 秒重驗指令。
**⑤驗收**：fixture 一律 `mktemp -d` hermetic（禁外部可變狀態——01:07 覆蓋事件實證）；驗收分「行為改變探針」（必須在舊程式上失敗）與「回歸保留測試」（新舊都過），逐項標註。

## 3. 變更清單

### C1（P0）— 腳本硬化（`review-state.mjs`、`spec-freeze.mjs`）
1. **F14①**：兩條 early return（`:89-92`、`:94-97`）改 exit 20——「沒狀態檔」「S3 零輪次」是未通過的證據，不是警告。
2. **F13**：`--blockers` 只收 `Number.isSafeInteger(n) && 1 <= n <= 50`（0/NaN/負/非整數/超上限 ⇒ rc=2）且強制 `--why`；round 記 `manual_override: {declared, why}`，`--status` 列 `manual_override_rounds`。可人工宣告「有幾條」，不能人工宣告「沒有問題」。
3. **F12**：`record()` 入場守衛——`rounds.length >= MAX_ROUNDS` 時在讀 log 之前 exit 20 且**不寫任何東西**（R3 本身照舊記錄後依結果 exit）。
4. **F16**：`save()` 改寫 `.tmp` 後 `renameSync`；改狀態指令（`--start/--record/--resolve/--freeze/--revise`）全程持 `openSync(LOCK,'wx')` 鎖（`.claude/review-state.lock`、`.claude/spec-freeze.lock`，內容 `{pid,at}`）；EEXIST ⇒ rc=2 fail-closed（>60s 註「疑似殘留請人工刪」，不自動搶）；入場守衛在鎖內重讀後執行。README gitignore 清單（`:74-78`）加兩個 `.lock`。
5. **F2**：`--start` 蓋「未收口（最後一輪有未 resolve 的 BLOCKER）或 STOP 狀態（`invocation_failures > MAX_INVOCATION_RETRY`，即使零有效輪）」的 run 需 `--force --why`，否則 rc=2 狀態不動；通過時新狀態記 `restarted_over: {prev_task, prev_rounds, open_ids, why, at}`。
6. **提示輸出（採納，非閘門）**：`--status` 在「最後一輪有 BLOCKER 且全部經 `--resolve`、其後沒有更新的複審輪」時多印一行：「已全部標記處理，但未經複審輪確認——green_allowed 是弱訊號，建議送複審（見 design-green-semantics）」。**不改變 `green_allowed` 的值、不影響任何 exit code。** 採納理由：F10 操作者已在人工做這個判斷，一行輸出零新表面。

**驗收**（hermetic；除 (b2) 外皆為行為改變探針）：
- (a) 無 review-state.json → `--freeze` rc 20；S3 零輪次 → rc 20。
- (b) `--blockers` 取 `0`/`abc`/`-1`/`1.5`/`51` → rc=2 狀態不動；`--blockers 2` 無 `--why` → rc=2；有 → `manual_override` 入檔。(b2) [保留] `--blockers 3 --why x` 正常計輪。
- (c) R1..R3 各記有 BLOCKER 一輪（R3 → rc 20），再 record R4 → rc 20 **且狀態檔逐位元組不變**。
- (d) 預置 lock → `--record` rc 2 狀態不動；正常執行完無 `.tmp`/`.lock` 殘留。
- (e) `--start` 蓋未收口 run：無 `--force` → rc=2 原狀態逐位元組不變；STOP 狀態（零有效輪、retry 耗盡）同；帶 `--force --why` → `restarted_over` 入檔。
- (f) resolve 全部後 `--status` 輸出含「弱訊號」提示行，且 `green_allowed` 值與現行為相同。
- (g) 負控組（逐一改壞→紅→還原）：early return 還原 →(a) 紅；整數驗證移除 →(b) 紅；入場守衛移除 →(c) 紅；鎖移除 →(d) 紅；force 條件移除 →(e) 紅；提示移除 →(f) 紅。

### C2（P0）— skill 散文可攜化＋lint
14 行（3 定義＋11 呼叫點，行號見 F5）改 function 形式：`rs() { node "${CLAUDE_PLUGIN_ROOT}/scripts/review-state.mjs" "$@"; }`（`sf()` 同理；`CX=(...)`/`"${CX[@]}"` bash/zsh 皆合法，保留）。`validate-plugin.mjs` 加 lint：只掃 SKILL.md 的 ```` ```bash ````/```` ```sh ```` fenced block，行匹配 `^\s*\$[A-Za-z_]+(\s|$)` ⇒ 報錯；pre-push 自動跑。SKILL 各加一行理由（zsh 不分詞）。
**驗收**：(a) `grep -rEn '^\s*\$(RS|SF) ' plugins/` 零命中；(b) [負控] 塞回 `$RS --status S3` → validate-plugin 紅 → 還原；(c) `bash -c`/`sh -c` 實跑可用；zsh [未本機實測] → §4-Mac 收尾；(d) 明寫限界：lint 守已知模式類，`eval`/backtick 不在守備。

### C4（P1）— `scripts/doctor.mjs`（誠實範圍）
當下狀態查詢，不寫檔不累積。JSON＋exit code：`platform`；`node`(≥18)；`codex`(PATH/版本)；`codex_config_drift`（與釘值不同 ⇒ WARN 非 FAIL——F4/F15）；`pipeline_json`（復用 `lib/validate-config.mjs`）；`plugin_pin`（列 `spec-pipeline@sdd` **所有 scope entries** 的 sha+installPath）。`--probe-codex`（opt-in，花 token）：帶釘值旗標實跑 smoke test，**`spawnSync` 取雙串流**，斷言 rc=0、stdout=`ok`、**stderr 含 `model: gpt-5.6-sol` 與 `reasoning effort: xhigh`**（F15：banner 在 stderr）。
能力邊界（它只銷這些）：env 前提 ✅；登入＋釘值生效 ✅（probe）；zsh 片段 ❌（C2 lint＋owner Mac 實跑）；宿主能力（Agent/SendMessage）❌（主對話看自身工具清單）；Mac 完整流程 ❌（本次不驗收）。
**驗收**：(a) WSL 實跑 JSON 齊備 exit 0；(b) [負控] 空 PATH → `codex.ok:false` exit 1；(c) `plugin_pin` entry 數 == 檔案實際數；(d) probe 的 stderr 斷言在腳本內（parse 不到 ⇒ FAIL）；probe 屬 owner 實跑項。

### C5（P1）— 契約事實更正＋邊界條款
① `codex-review/SKILL.md:8` 改「2026-09-01 重驗，codex-cli 0.144.1」附 `codex --version`；② `:176-184` 改寫：config 實際 gpt-5.5/medium，F15 為主證（附重驗指令）、F4 輔證，補「banner 在 stderr」；③ `:124-130` v0.150 軼事換成本機可驗事實（`^codex$` 5 次）；④ `HANDOFF.md:66` 改不綁 config 值；⑤ 契約加規則：帶版本/機器狀態的事實必附 30 秒重驗指令（原則④）；⑥ README.md:5-7 後加：「⚠️ 邊界：閘門約束的是進入流程的任務；plugin 無法強制任務進入流程，完全繞過不產生訊號、事後也難以稽核。走不走流程是 owner 的決定。」
**驗收**：(a) `grep -rn '0\.150' plugins/spec-pipeline/skills plugins/spec-pipeline/commands` 零命中——**刻意不掃 `tests/`**：`tests/fixtures-codex-transcript.log:1` 是假綠回歸的真實逐字稿，不可改，且是「事實會過期」的活標本；(b) 三段各含可複製重驗指令；(c) README grep「進入流程的任務」命中；(d) validate-plugin 綠。

### C6（P2）— `README.md:170`、`HANDOFF.md:26` 去掉「44 個 case」硬編數字。驗收：grep 零命中。

### C7（P2）— round 加 `log_sha256`（完整 64 hex）＋`log_bytes`（raw byte 長度＝讀檔 Buffer `.length`）。驗收：(a) 與 Node `crypto` 實算一致（不用 `sha256sum`，macOS 無）；(b) 非 ASCII log：bytes > 字元數；(c) [保留] 舊狀態檔讀取不炸。

### C8（P1）— 入口停點雙選項（owner 裁示：採規劃委派、否決整段委派）
`spec-pipeline/SKILL.md:24-30`：停點改雙選——選項一（預設）owner 切 `/model`；選項二 owner 授權派 `Agent(model:<指定>)` 做 S1+S2，回報後主對話續走 S3，監督職責全留主對話。**選項二條件式**：主對話工具清單確有 `Agent` 與 `SendMessage` 才提供（SendMessage 是宿主能力，session 級事實，不得宣稱普遍可用）。四停止點以「回報即終止 → 問 owner → `SendMessage` 續跑」處理。明寫整段委派被否決及理由（職責 #3/#5，`:161-163`）。commands 兩檔不動。
**驗收**：(a) grep 命中「授權派」「SendMessage」「工具清單」；(b) grep 命中否決段；(c) commands diff 為空；(d) validate-plugin 綠。

## 4. 部署與生效驗證

1. `node scripts/validate-plugin.mjs && node --test plugins/spec-pipeline/tests/*.test.mjs` 全綠（本 repo `verify_cmd`；改動在 `plugins/**` ⇒ F0 必 FULL）。
2. bump `plugin.json` 0.1.0→0.2.0＋同步 `marketplace.json`；commit＋push（pre-push 重跑 1）。
3. **部署前基準探針**（原則⑤）：對 cache 路徑跑 `--blockers abc` 序列 → 預期舊行為 CLEAR/rc 0。
4. owner 手動 `/plugin update` → **重啟 session**（舊 SKILL 已載入的 session 不會變）。
5. 生效判定：`installed_plugins.json` 的 `spec-pipeline@sdd` **每個 entry**（user+project）sha == 新 HEAD、installPath 含 `/0.2.0`；`diff -rq` cache vs repo 只剩 `.in_use`；**部署後對照探針**：同 3 → rc=2（成對證明部署改變了行為）。
6. **Mac 端**（owner）：`/plugin update` → doctor（比對兩機 pin）→ `--probe-codex` → `node --test <cache>/tests/*.test.mjs`（整套在 Mac 的 Node 實跑）→ session 實跑 `rs --status S3`。明確不含 Mac 完整 S0→S5。

## 5. 明確不做／殘餘

不做：green 語意任何變更（→ 第二份檔案）；遙測/retro；harness hook 強制入流程（無法辨識任務歸屬；README 邊界條款如實記載）；放寬哨兵解析器；動 `codex-review/SKILL.md:33` 明寫呼叫點；改 MAX_ROUNDS 數值。
殘餘：green_allowed 弱訊號（§0，僅提示不設閘）；G1-G4 四缺口靠人工紀律（詳見第二份檔案）；C2 lint 啟發式限界；鎖不自動搶（人工刪）；Mac 完整流程未驗收；probe 花 token。

## 6. 裁示與出處

六輪審查（run1 R1 7B→R2 4B→R3 5B stop；run2 R1 7B→R2 2B→R3 stop）完整歷史、四缺口失敗史、green 語意候選設計與五輪數據，全部在 `plan/self-improve/design-green-semantics-2026-09-01.md`。owner 四次裁示：game_01 移出驗收；C8 採規劃委派否決整段委派；縮小範圍；**拆分——本檔為定案批，green 語意延後另走流程**。本檔項目的共同特徵：六輪無一被實質反對，全部只依賴「既成事實搬運」型輸入，無語意主張。
