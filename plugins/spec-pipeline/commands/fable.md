---
description: 用 Fable 規劃 → 規格 → Codex 審 → 實作 → Codex 審（難任務）
argument-hint: '[--full] <任務描述>'
---

# /fable

用 **Fable** 規劃這件事，然後走完整的規格驅動流程。

⚠️ **流程主體在 `spec-pipeline` skill —— 載入它並照著做。**
指令只負責帶入「指定的規劃模型 = Fable」這一件事，
不要在這裡複製流程內容（三個指令共用同一份，複製就會漂移）。

指定模型：**Fable**

