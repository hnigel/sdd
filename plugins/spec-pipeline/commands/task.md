---
description: 走完整流程，由 S0 判難度並建議規劃模型
argument-hint: '[--full] <任務描述>'
---

# /task

用 **（不指定）** 規劃這件事，然後走完整的規格驅動流程。

⚠️ **流程主體在 `spec-pipeline` skill —— 載入它並照著做。**
指令只負責帶入「指定的規劃模型 = （不指定）」這一件事，
不要在這裡複製流程內容（三個指令共用同一份，複製就會漂移）。

指定模型：**（不指定）**

沒有指定模型 ⇒ S0 判 `normal`/`hard` 之後**講出建議並問 owner**，不自己決定。

